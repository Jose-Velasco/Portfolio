import { SkillIconLocalUrlPipe } from './skill-icon-local-url.pipe';

describe('SkillIconLocalUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillIconLocalUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
